# Enhanced interactive terminal UI. The CLI falls back to the standard library
# when these packages are unavailable or input/output is redirected.
prompt-toolkit>=3.0.51,<4
rich>=13.9,<15
openai>=2.6,<3
pydantic>=2.11,<3
