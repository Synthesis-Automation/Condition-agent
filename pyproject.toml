[project]
name = "chemtools-project"
version = "0.1.1"
description = "Chemistry tools for condition recommendation (FastAPI)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
chem-registry = "chemtools.rule_scdb_matcher.cli:main"
scdb = "chemtools.rule_scdb_matcher.cli:main"

[project.optional-dependencies]
molpipeline = [
    "molpipeline>=0.10.3",
]
