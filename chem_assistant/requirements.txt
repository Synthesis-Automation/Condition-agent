# LangChain/LangGraph dependencies for ChemTools integration
# Install with: pip install -r lang_chain/requirements.txt

# Core LangChain packages
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.0.5

# LangGraph for ReAct agents
langgraph>=0.0.20

# Utilities
python-dotenv>=1.0.0

# Note: The parent requirements.txt already includes chemtools dependencies
# This file only adds LangChain-specific requirements
