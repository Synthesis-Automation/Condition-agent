# MCP Server Demo - Requirements
#
# Install with: pip install -r requirements-mcp.txt
#
# This includes the minimal dependencies needed to run the
# SMILES Normalizer MCP server demo.

# MCP (Model Context Protocol) SDK
mcp>=0.9.0

# Core dependencies from main project (if not already installed)
# These are needed for the chemtools.smiles module
rdkit-pypi>=2022.9.1  # Chemistry toolkit
pydantic>=2.0.0       # Data validation
